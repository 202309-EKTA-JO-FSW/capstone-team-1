/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "firebasestorage.googleapis.com",
      "illustoon.com",
      "www.pngall.com",
    ],
  },
};

export default nextConfig;
